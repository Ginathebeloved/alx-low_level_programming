#include <stdio.h>
/**
 * main - Determines if a number is positive, negative or zero.
 *Description: 'get the last difit of a number'
 * Return: Always 0
 */

int main(void)
{
		putchar(65);
		putchar('\n');
		return (0);
}
